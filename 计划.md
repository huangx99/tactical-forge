# Tactical Forge 架构重构计划

## 1. 当前问题诊断

### 1.1 两套并行运行时

| | Engine (PixiJS) | PlayMode (Canvas 2D) |
|---|---|---|
| 输入 | InputSystem | 自己写的 keydown listener |
| 移动 | PlayerSystem (硬编码 WASD) | gameLoop 内硬编码 |
| 碰撞 | PhysicsSystem | 自己写的 checkCollision |
| 渲染 | RenderSystem (PixiJS) | ctx.fillRect |
| 蓝图 | ScriptSystem (空壳) | 无 |

PlayMode 完全绕过了 Engine，重新实现了一切。两套代码互不相关。

### 1.2 ScriptSystem 是空壳

```typescript
// packages/engine/src/systems/ScriptSystem.ts
update(_entities: Entity[], _dt: number): void {
  // Blueprint execution will be implemented in Phase 4
}
```

蓝图编辑器能画节点图，但运行时不存在，蓝图无法执行。

### 1.3 没有事件系统

蓝图的 onInteract、onCollision、onTimer、onDeath 等事件节点没有触发机制。系统间通信全靠硬编码调用。

### 1.4 资产与蓝图的连接断裂

| 资产 | 蓝图节点 | Inspector 组件 | 实际可用 |
|------|---------|---------------|---------|
| 物品 Item | addItem, removeItem, hasItem | Inventory, Equipment | 部分 |
| 技能 Skill | 无 | SkillBar | 部分 |
| 状态效果 StatusEffect | applyStatus (有节点无执行) | 无组件 | **不可用** |
| 掉落表 LootTable | 无 | 无组件 | **不可用** |

### 1.5 输入硬编码

PlayerSystem 硬编码 WASD/方向键。没法通过蓝图自定义输入映射，没法让 NPC 通过蓝图控制移动。

---

## 2. 目标架构

核心原则：**一切逻辑由蓝图驱动**（类似 Unreal Engine）

```
┌──────────────────────────────────────────────────┐
│                    EventBus                       │
│  input.*  collision.*  entity.*  timer.*         │
│  dialogue.*  combat.*  inventory.*               │
└────────┬─────────────────────────────────────────┘
         │ emit / subscribe
         ▼
┌──────────────────────────────────────────────────┐
│                ScriptSystem                       │
│  ┌────────────────────────────────────────┐      │
│  │  BlueprintRuntime (节点图解释器)         │      │
│  │  - 事件节点 → 执行入口                   │      │
│  │  - 条件节点 → 分支判断                   │      │
│  │  - 动作节点 → 修改组件/发射事件           │      │
│  │  - 流程节点 → 顺序/延迟/循环/引用         │      │
│  └────────────────────────────────────────┘      │
│  ┌────────────────────────────────────────┐      │
│  │  BlueprintContext (每个实体一个)          │      │
│  │  - 变量黑板                              │      │
│  │  - 活跃定时器                            │      │
│  │  - 等待队列                              │      │
│  └────────────────────────────────────────┘      │
└────────┬─────────────────────────────────────────┘
         │ read / write
         ▼
┌──────────────────────────────────────────────────┐
│              Entity Components                    │
│  Transform  Health  Stats  Inventory  Equipment   │
│  SkillBar  StatusEffects  Collider  Blueprint     │
│  LootTable  Dialogue  PlayerController            │
└──────────────────────────────────────────────────┘
```

### 设计要点

**1. EventBus 是唯一通信通道**
- 系统发射事件，蓝图监听事件
- 蓝图也可以发射事件，由系统或其它蓝图处理
- 不使用系统间直接调用

**2. 所有行为 = 蓝图**
- 玩家移动 = 监听 `input.move` 事件的蓝图
- NPC 对话 = 监听 `entity.interact` 事件的蓝图
- 敌人 AI = 监听 `entity.tick` 或 `entity.seePlayer` 的蓝图
- 宝箱 = 监听 `entity.interact` 的蓝图，执行 `rollLoot` + `addItem`

**3. 资产是数据，蓝图是逻辑**
- 物品/技能/状态/掉落表 定义在编辑器中
- 蓝图节点引用这些数据 ID
- 运行时通过 ID 查找数据并执行

**4. PlayMode 运行在 Engine 上**
- 废弃 PlayMode.tsx 中的独立游戏循环
- PlayMode 创建 Engine 实例，加载场景实体
- 编辑器和试玩共享同一套引擎代码

---

## 3. 详细设计

### 3.1 EventBus

```typescript
// packages/engine/src/events/EventBus.ts

interface GameEvent {
  type: string;
  data: Record<string, unknown>;
  timestamp: number;
  sourceId?: string; // 发射者 entityId
}

class EventBus {
  private handlers = new Map<string, Set<(e: GameEvent) => void>>();

  on(pattern: string, handler: (e: GameEvent) => void): void;
  off(pattern: string, handler: (e: GameEvent) => void): void;
  emit(type: string, data: Record<string, unknown>): void;
}
```

#### 事件命名规范

```
input.{action}         — 输入事件
entity.{event}         — 实体生命周期
collision.{type}       — 碰撞事件
timer.complete         — 定时器完成
dialogue.{event}       — 对话事件
inventory.{event}      — 背包事件
combat.{event}         — 战斗事件
scene.{event}          — 场景事件
```

#### 核心事件列表

| 事件 | 数据 | 说明 |
|------|------|------|
| `input.move` | `{ x, y }` | 归一化移动向量 |
| `input.interact` | `{}` | 交互键按下 |
| `input.jump` | `{}` | 跳跃键按下 |
| `input.attack` | `{}` | 攻击键按下 |
| `entity.start` | `{ entityId }` | 实体创建/场景加载 |
| `entity.death` | `{ entityId, killerId? }` | 实体死亡 |
| `entity.damaged` | `{ entityId, amount, sourceId }` | 实体受伤 |
| `collision.enter` | `{ entityId, otherId }` | 碰撞开始 |
| `collision.exit` | `{ entityId, otherId }` | 碰撞结束 |
| `timer.complete` | `{ entityId, timerId }` | 定时器到期 |
| `dialogue.start` | `{ entityId, text, choices }` | 开始对话 |
| `dialogue.end` | `{ entityId }` | 对话结束 |
| `dialogue.choice` | `{ entityId, index }` | 选择了选项 |
| `inventory.changed` | `{ entityId }` | 背包变化 |
| `scene.start` | `{ sceneId }` | 场景加载 |
| `combat.turnStart` | `{ entityId }` | 回合开始 |
| `combat.turnEnd` | `{ entityId }` | 回合结束 |

### 3.2 蓝图运行时

```
ScriptSystem
├── EventBus 引用
├── BlueprintRuntime（解释器）
│   ├── 根据事件类型找到匹配的事件节点
│   ├── 从事件节点的输出端口开始遍历
│   ├── 对每个节点调用对应的 NodeExecutor
│   └── 支持异步操作（delay 等）
└── 每个实体的 BlueprintContext
    ├── variables: Map<string, unknown>    // 局部变量
    ├── activeTimers: Timer[]              // 活跃定时器
    └── pendingDelays: Delay[]             // 延迟等待队列
```

#### 执行模型

```
1. EventBus 发射事件 "input.interact"
2. ScriptSystem.handleEvent(event)
3. 遍历所有有 Blueprint 组件的实体
4. 在每个实体的蓝图里找 type === "event/onInteract" 的节点
5. 找到 → executeFromNode(blueprint, eventNode, 'out', event.data)
6. 沿输出边走到下一个节点
7. 调用 nodeExecutors[node.type](context, node, inputPort, data)
8. 动作节点执行完从它的输出端口继续
9. 条件节点根据结果选择 true/false 端口继续
10. 延迟节点把回调加入 pendingDelays，时间到了再继续
```

#### NodeExecutor 接口

```typescript
type NodeExecutor = (
  runtime: BlueprintRuntime,
  context: BlueprintContext,
  node: BlueprintNode,
  inputPort: string,
  eventData: Record<string, unknown>
) => void;
```

#### 关键节点执行逻辑

**事件节点** — 入口，不执行逻辑，直接继续
**动作节点** — 执行操作（修改组件/发射事件），然后从 `out` 端口继续
**条件节点** — 判断条件，从 `true` 或 `false` 端口继续
**流程/sequence** — 按 `out1` → `out2` → `out3` 顺序执行
**流程/delay** — 记录到 pendingDelays，在 update 中倒计时，到期后继续
**流程/loop** — 计数循环，每次从 `body` 端口执行，最后从 `done` 继续
**流程/blueprintRef** — 切换到被引用的蓝图的事件节点继续执行（详见 3.8）

### 3.3 输入系统重构

#### 输入动作抽象（类似 UE InputAction）

```typescript
interface InputAction {
  name: string;
  type: 'digital' | 'axis1D' | 'axis2D';
  bindings: { key: string; scale: number }[];
}
```

#### 默认输入动作

| 动作 | 按键 | 触发事件 |
|------|------|---------|
| `move_right` | D / 右箭头 | `input.move` |
| `move_left` | A / 左箭头 | `input.move` |
| `move_up` | W / 上箭头 | `input.move` |
| `move_down` | S / 下箭头 | `input.move` |
| `interact` | E / Enter | `input.interact` |
| `jump` | 空格 | `input.jump` |
| `attack` | J | `input.attack` |
| `skill_1` | 1 | `input.skill` { slot: 0 } |
| `skill_2` | 2 | `input.skill` { slot: 1 } |
| `skill_3` | 3 | `input.skill` { slot: 2 } |
| `skill_4` | 4 | `input.skill` { slot: 3 } |

#### InputActionMapper（包在 InputSystem 外层）

现有 InputSystem 保持不变（原始按键追踪），新增 InputActionMapper 读取 InputSystem 状态并发射事件：

```typescript
class InputActionMapper {
  constructor(private input: InputSystem, private eventBus: EventBus) {}

  private actions: InputAction[] = [...]; // 默认 + 用户自定义

  update(): void {
    // 1. 遍历所有 axis2D 动作，合并为 input.move
    // 2. 遍历 digital 动作，justPressed 时发射对应事件
    // 3. 遍历 axis1D 动作，发射 input.skill 等
  }
}
```

这样不破坏现有 InputSystem 的 API，蓝图和旧代码可以共存。

#### InputSystem 流程

```
1. 捕获原生 keydown/keyup（InputSystem，保持不变）
2. InputActionMapper.update() 读取 InputSystem 状态
3. 合并同轴输入 → emit("input.move", { x, y })
4. 数字动作 justPressed → emit("input.interact" / "input.jump" / "input.attack" / "input.skill")
```

### 3.4 Entity 组件变更

#### 新增组件

**Blueprint 组件** — 挂载蓝图到任意实体
```typescript
class BlueprintComponent extends Component {
  static typeName = 'blueprint';
  blueprintIds: string[] = [];   // 可挂多个蓝图
  variables: Record<string, unknown> = {};  // 实例变量
}
```

**Loot 组件** — 挂载掉落表到实体
```typescript
class Loot extends Component {
  static typeName = 'loot';
  lootTableId: string | null = null;
}
```

**Tag 组件** — 实体标签，用于蓝图碰撞过滤等
```typescript
class Tag extends Component {
  static typeName = 'tag';
  tags: string[] = [];  // 如 ['player', 'enemy', 'chest', 'npc']
}
```

#### 已有但需集成到 Inspector 的组件

- `StatusEffects` — 已存在于 engine，但 Inspector 不支持

### 3.5 资产联动蓝图节点

#### 物品相关

| 节点 | 类型 | 说明 |
|------|------|------|
| `action/addItem` | action | 向背包添加物品（已有） |
| `action/removeItem` | action | 从背包移除物品（已有） |
| `condition/hasItem` | condition | 检查是否有物品（已有） |
| `action/useItem` | action | 使用消耗品 |
| `action/equipItem` | action | 装备物品 |
| `action/unequipItem` | action | 卸下装备 |
| `action/openShop` | action | 打开商店界面 |

#### 技能相关

| 节点 | 类型 | 说明 |
|------|------|------|
| `action/castSkill` | action | 释放技能 |
| `condition/skillReady` | condition | 检查技能冷却 |
| `condition/hasSkill` | condition | 检查是否拥有技能 |
| `action/learnSkill` | action | 学习技能 |
| `action/setCooldown` | action | 设置冷却 |

#### 状态效果相关

| 节点 | 类型 | 说明 |
|------|------|------|
| `action/applyStatus` | action | 施加状态（已有，需补充执行逻辑） |
| `action/removeStatus` | action | 移除状态 |
| `condition/hasStatus` | condition | 检查是否有某状态 |
| `action/clearAllStatus` | action | 清除所有状态 |

#### 掉落表相关

| 节点 | 类型 | 说明 |
|------|------|------|
| `action/rollLoot` | action | 按掉落表随机生成物品 |
| `action/spawnLoot` | action | 生成掉落物到世界 |

### 3.6 PlayMode 重构

#### 当前问题
PlayMode.tsx 有一套完整的独立游戏循环，重复实现了 Engine 的功能。

#### 重构后

```typescript
// PlayMode.tsx 重构后
export function PlayMode() {
  const engineRef = useRef<Engine | null>(null);

  useEffect(() => {
    // 1. 创建 Engine 实例（复用 PixiJS + 所有 System）
    const engine = new Engine(canvas, config);
    engine.eventBus.on('*', debugLog); // 调试用

    // 2. 从当前场景加载实体到 Engine
    for (const obj of sceneObjects) {
      const entity = createEntityFromObject(obj);
      engine.addEntity(entity);
    }

    // 3. 引擎接管游戏循环
    engine.start();
    engineRef.current = engine;

    return () => engine.destroy();
  }, [activeScene]);

  return <canvas ref={canvasRef} />;
}
```

### 3.7 默认蓝图模板

创建内置蓝图（存储在 localStorage 或预设 JSON）：

| 模板 | 事件监听 | 行为 |
|------|---------|------|
| **PlayerController** | `input.move`, `input.jump`, `input.interact` | 移动、跳跃、触发交互碰撞检测 |
| **SimpleNPC** | `entity.interact` | 显示对话，关闭后继续闲置 |
| **EnemyPatrol** | `entity.start`, `entity.seePlayer`, `entity.death` | 巡逻路径点，发现玩家后追击，死亡掉落 |
| **EnemyAggressive** | `entity.start`, `entity.seePlayer`, `entity.death` | 站岗，发现玩家直接攻击 |
| **Shopkeeper** | `entity.interact` | 显示商店菜单 |
| **TreasureChest** | `entity.interact` | 检查标记（是否已开），rollLoot，标记已开 |
| **QuestGiver** | `entity.interact` | 检查任务标记，显示不同对话 |
| **Door/Portal** | `entity.interact` | 传送到指定位置或切换场景 |
| **PickableItem** | `entity.interact` | 添加物品到背包，删除自身 |
| **DamagableProp** | `entity.damaged` | 受伤，死亡时掉落 |

### 3.8 BlueprintRef 跨蓝图引用

#### 数据结构

```typescript
// flow/blueprintRef 节点的 data
interface BlueprintRefData {
  targetBlueprintId: string;      // 引用的蓝图 ID
  entryEvent?: string;            // 入口事件类型，默认 'event/onStart'
  passVariables?: Record<string, string>;  // 变量映射: { localVar: callerVar }
}
```

#### 执行模型

```
1. 执行到 blueprintRef 节点
2. 查找 targetBlueprintId 对应的蓝图定义
3. 找到入口事件节点（默认 event/onStart，可通过 entryEvent 指定）
4. 复制调用者的 BlueprintContext（共享同一个 variables 引用，非深拷贝）
5. 如果有 passVariables，将调用者变量映射到被引用蓝图的变量名
6. 从入口节点开始执行被引用蓝图
7. 被引用蓝图执行完毕（无更多节点或遇到终端），控制流不返回调用者
```

#### 作用域规则

- **共享变量**：被引用蓝图读写的是调用者 context 的同一个 variables 对象
- **定时器归属**：被引用蓝图创建的定时器归属调用者实体
- **无返回机制**：blueprintRef 是"fire and forget"，不支持 return

#### 循环引用防护

```typescript
// BlueprintRuntime 中维护调用栈
executeBlueprint(blueprint, context, event) {
  if (context.callStack.includes(blueprint.id)) {
    console.warn(`Circular blueprint reference: ${blueprint.id}`);
    return; // 中断
  }
  context.callStack.push(blueprint.id);
  // ... 执行
  context.callStack.pop();
}
```

#### 多蓝图执行顺序

当一个实体挂载多个蓝图（`blueprintIds: string[]`），事件触发时：
- 按 `blueprintIds` 数组顺序依次执行
- 每个蓝图独立执行，不共享执行状态
- 但共享同一个 `BlueprintContext`（变量黑板）

### 3.9 GameFlags 全局状态

```typescript
// packages/engine/src/state/GameFlags.ts
class GameFlags {
  private flags = new Map<string, boolean>();
  private variables = new Map<string, unknown>();

  getFlag(key: string): boolean { return this.flags.get(key) ?? false; }
  setFlag(key: string, value: boolean): void { this.flags.set(key, value); }

  getVariable(key: string): unknown { return this.variables.get(key); }
  setVariable(key: string, value: unknown): void { this.variables.set(key, value); }

  toJSON(): object { ... }
  fromJSON(data: object): void { ... }
}
```

- Engine 持有一个 GameFlags 实例
- 蓝图节点通过 `runtime.gameFlags` 访问
- `condition/checkFlag` → `runtime.gameFlags.getFlag(node.data.flag)`
- `action/setFlag` → `runtime.gameFlags.setFlag(node.data.flag, true)`
- 支持序列化（用于存档）

### 3.10 Dialogue 运行时

```typescript
// packages/engine/src/systems/DialogueManager.ts
class DialogueManager {
  private activeDialogue: ActiveDialogue | null = null;

  constructor(private eventBus: EventBus) {
    eventBus.on('dialogue.start', this.onStart.bind(this));
    eventBus.on('dialogue.choice', this.onChoice.bind(this));
  }

  startDialogue(entityId: string, text: string, choices?: string[]): void {
    this.activeDialogue = { entityId, text, choices, resolved: false };
    // 事件由 UI 层监听并渲染对话框
  }

  choose(index: number): void {
    if (!this.activeDialogue) return;
    this.activeDialogue.resolved = true;
    this.eventBus.emit('dialogue.choice', {
      entityId: this.activeDialogue.entityId,
      index,
    });
    this.eventBus.emit('dialogue.end', {
      entityId: this.activeDialogue.entityId,
    });
    this.activeDialogue = null;
  }

  isActive(): boolean { return this.activeDialogue !== null; }
  getActive(): ActiveDialogue | null { return this.activeDialogue; }
}
```

- `action/showDialogue` 节点调用 `dialogueManager.startDialogue()`
- UI 层监听 `dialogue.start` 事件渲染对话框
- 用户点击选项 → 调用 `dialogueManager.choose(index)`
- `dialogue.choice` 事件回到蓝图，蓝图根据 index 走不同分支

### 3.11 Entity Spawning

```typescript
// packages/engine/src/spawn/EntitySpawner.ts
class EntitySpawner {
  constructor(private engine: Engine) {}

  spawnFromPrefab(prefabId: string, x: number, y: number): Entity | null {
    const prefab = this.prefabRegistry.get(prefabId);
    if (!prefab) return null;

    const entity = new Entity(generateId());
    for (const comp of prefab.components) {
      entity.addComponent(Component.fromJSON(comp));
    }
    // 覆盖位置
    const transform = entity.getComponent<Transform>('transform');
    if (transform) { transform.x = x; transform.y = y; }

    this.engine.addEntity(entity);
    this.engine.eventBus.emit('entity.start', { entityId: entity.id });
    return entity;
  }
}
```

- `action/spawnObject` 节点调用 `spawner.spawnFromPrefab(prefabId, x, y)`
- Prefab 注册表由编辑器的场景对象数据填充
- 新实体自动加入 Engine 的系统循环

### 3.12 Audio Manager

```typescript
// packages/engine/src/audio/AudioManager.ts
class AudioManager {
  private audioContext: AudioContext | null = null;
  private buffers = new Map<string, AudioBuffer>();
  private masterVolume = 1;

  async loadSound(id: string, url: string): Promise<void> { ... }
  playSound(id: string, volume?: number): void { ... }
  stopAll(): void { ... }
  setMasterVolume(v: number): void { ... }
}
```

- `action/playSound` 节点调用 `audioManager.playSound(id)`
- 懒初始化 AudioContext（需要用户交互后才能创建）
- 资源加载由编辑器资产系统提供 URL

### 3.13 Save/Load

```typescript
// packages/engine/src/save/SaveManager.ts
interface SaveData {
  version: number;
  timestamp: number;
  entities: Array<{ id: string; components: Record<string, unknown> }>;
  gameFlags: { flags: Record<string, boolean>; variables: Record<string, unknown> };
  blueprintContexts: Array<{ entityId: string; variables: Record<string, unknown> }>;
  currentScene: string;
}

class SaveManager {
  constructor(private engine: Engine) {}

  save(): SaveData {
    const entities = this.engine.getEntities().map(e => ({
      id: e.id,
      components: e.toJSON(),
    }));
    return {
      version: 1,
      timestamp: Date.now(),
      entities,
      gameFlags: this.engine.gameFlags.toJSON(),
      blueprintContexts: this.engine.scriptSystem.serializeContexts(),
      currentScene: this.engine.currentSceneId,
    };
  }

  load(data: SaveData): void { ... }

  saveToSlot(slot: number): void {
    localStorage.setItem(`save_${slot}`, JSON.stringify(this.save()));
  }

  loadFromSlot(slot: number): boolean {
    const raw = localStorage.getItem(`save_${slot}`);
    if (!raw) return false;
    this.load(JSON.parse(raw));
    return true;
  }
}
```

### 3.14 碰撞职责划分

| 职责 | PhysicsSystem | PlayerSystem |
|------|--------------|-------------|
| 碰撞检测 | **全部由 PhysicsSystem 负责** | 不做碰撞检测 |
| 碰撞事件 | 通过 EventBus 发射 `collision.enter/exit` | 不发射事件 |
| 碰撞响应 | push-apart（实体分离） | 不处理 |
| 移动 | 不处理 | **只做移动逻辑**（响应 input.move） |
| moveWithCollision | 移到 PhysicsSystem 或作为工具函数 | 从 PlayerSystem 移除 |

PhysicsSystem 更新后发射碰撞事件：
```typescript
// PhysicsSystem.update()
for (const pair of collisionPairs) {
  this.eventBus.emit('collision.enter', {
    entityId: pair.a.id,
    otherId: pair.b.id,
  });
}
```

### 3.15 StatusEffects Tick 归属

- **ScriptSystem 负责 tick 逻辑**（不是独立系统）
- ScriptSystem.update() 中遍历所有有 StatusEffects 组件的实体
- 对每个 active effect：
  - 递减 `remainingDuration`
  - 递增 `tickTimer`，达到 tickInterval 时触发效果（如回血、扣血）
  - 触发 `statusEffect.tick` 事件（蓝图可以监听）
  - duration 归零 → 移除效果，发射 `statusEffect.expire` 事件

### 3.16 多蓝图执行顺序

```
entity.blueprintIds = ['bp_move', 'bp_attack', 'bp_ai']

事件 input.move 触发时：
1. 按数组顺序遍历
2. 在 bp_move 中找 onInteract → 找到 → 执行
3. 在 bp_attack 中找 onInteract → 没找到 → 跳过
4. 在 bp_ai 中找 onInteract → 找到 → 执行

所有匹配的蓝图依次执行，互不干扰。
```

- 执行顺序 = 数组顺序
- 可通过 Inspector 拖拽排序
- 蓝图之间通过 EventBus 间接通信，不直接调用

### 3.17 事件时序与系统更新顺序

每帧的执行顺序：

```
┌─ InputSystem.update()        → 发射 input.*
├─ InputActionMapper.update()  → 合并轴输入，发射 input.move
├─ PlayerSystem.update()       → 响应 input.move（如果不用蓝图驱动）
├─ PhysicsSystem.update()      → 碰撞检测，发射 collision.*
├─ ScriptSystem.update()
│   ├─ 处理 pendingDelays（倒计时）
│   ├─ 处理 StatusEffects tick
│   ├─ 处理本帧所有待处理事件
│   │   ├─ input.* 事件 → 找匹配蓝图 → 执行
│   │   ├─ collision.* 事件 → 找匹配蓝图 → 执行
│   │   ├─ 蓝图执行中 emit 新事件 → 加入队列
│   │   └─ 继续处理直到队列为空（同帧传播）
│   └─ 每个实体最多执行 N 步（防无限循环）
├─ CameraSystem.update()
└─ RenderSystem.update()
```

同帧传播 vs 延迟：
- 默认：蓝图 emit 的事件在同一帧内继续处理
- 可选：`eventBus.emitDeferred(type, data)` 延迟到下一帧
- 防护：每个实体每帧最多执行 1000 个节点步，超出则 warn 并中断

### 3.18 蓝图变量 vs 组件数据边界

| 存储位置 | 用途 | 生命周期 | 示例 |
|---------|------|---------|------|
| **组件数据** | 实体的持久状态 | 实体存活期间，可序列化 | Health.current, Inventory.slots, Tag.tags |
| **BlueprintContext.variables** | 蓝图临时计算 | 单次执行或跨事件持久 | 循环计数器, 临时计算结果 |
| **BlueprintComponent.variables** | 蓝图实例配置 | 实体存活期间，可序列化 | NPC 对话阶段, 宝箱是否已开 |
| **GameFlags** | 全局游戏状态 | 跨实体、跨场景 | 主线进度, 全局开关 |

规则：
- 需要存档 → 存组件或 GameFlags
- 蓝图内部临时 → 存 context variables
- 蓝图配置参数 → 存 BlueprintComponent.variables

---

## 4. 实现阶段

### Phase A1: EventBus + 蓝图运行时核心

**目标**: 蓝图引擎层可工作，用单元测试验证

1. **EventBus** (`packages/engine/src/events/EventBus.ts`)
   - 实现 on / off / emit
   - 支持通配符匹配（`input.*`）
   - 支持 emitDeferred（延迟到下一帧）

2. **GameEvent 类型** (`packages/engine/src/events/GameEvent.ts`)

3. **BlueprintContext** (`packages/engine/src/blueprint/BlueprintContext.ts`)
   - 每实体一个：variables, activeTimers, pendingDelays, callStack

4. **BlueprintRuntime** (`packages/engine/src/blueprint/BlueprintRuntime.ts`)
   - 根据事件找事件节点
   - 沿边遍历节点图，调用 NodeExecutor
   - 循环引用检测（callStack）
   - 步数限制防无限循环

5. **NodeExecutors** (`packages/engine/src/blueprint/NodeExecutors.ts`)
   - 实现所有 22 个现有节点类型的执行逻辑
   - 包括 flow/blueprintRef 的跨蓝图引用逻辑

6. **Blueprint 组件** (`packages/engine/src/components/Blueprint.ts`)
   - blueprintIds, variables

7. **Tag 组件** (`packages/engine/src/components/Tag.ts`)
   - tags: string[]

8. **GameFlags** (`packages/engine/src/state/GameFlags.ts`)
   - 全局 flag 和变量存储

### Phase A2: ScriptSystem + 系统集成

**目标**: 事件驱动蓝图执行，ScriptSystem 替换空壳

1. **ScriptSystem 重写** (`packages/engine/src/systems/ScriptSystem.ts`)
   - 持有 EventBus、BlueprintRuntime、GameFlags
   - update 中处理 pendingDelays、StatusEffects tick、事件队列
   - 同帧事件传播 + 步数限制

2. **Engine 集成** (`packages/engine/src/Engine.ts`)
   - 创建 EventBus, GameFlags, BlueprintRuntime, DialogueManager, AudioManager
   - 传递给各 System
   - 暴露 entitySpawner

3. **InputActionMapper** (`packages/engine/src/input/InputActionMapper.ts`)
   - 包在 InputSystem 外层
   - 合并轴输入，发射 input.* 事件

4. **PhysicsSystem 发射事件** (`packages/engine/src/systems/PhysicsSystem.ts`)
   - 碰撞对通过 EventBus 发射 collision.enter/exit

### Phase A3: Runtime Managers + PlayMode 重构

**目标**: PlayMode 跑在 Engine 上，管理器可用

1. **DialogueManager** (`packages/engine/src/dialogue/DialogueManager.ts`)
   - 对话生命周期管理，事件桥接

2. **EntitySpawner** (`packages/engine/src/spawn/EntitySpawner.ts`)
   - 从 Prefab 创建实体，加入 Engine

3. **AudioManager** (`packages/engine/src/audio/AudioManager.ts`)
   - 基础音频播放

4. **SaveManager** (`packages/engine/src/save/SaveManager.ts`)
   - 状态序列化/反序列化，slot 存取

5. **PlayMode 重构** (`apps/editor/src/canvas/PlayMode.tsx`)
   - 使用 Engine 替代自定义游戏循环
   - 从场景数据创建实体
   - 集成 DialogueManager UI

### Phase B: 输入系统 + 玩家控制蓝图化

**目标**: 玩家移动通过蓝图驱动，非硬编码

1. **InputSystem 重构** (`packages/engine/src/systems/InputSystem.ts`)
   - 输入动作抽象
   - 合并同轴输入
   - 发射 `input.*` 事件到 EventBus

2. **PlayerSystem 改造**
   - 保留碰撞移动能力（moveWithCollision）
   - 移除硬编码键位读取
   - 改为响应 `input.move` 事件（通过蓝图调用）

3. **默认 PlayerController 蓝图**
   - 监听 `input.move` → 更新 Transform
   - 监听 `input.jump` → 设置垂直速度
   - 监听 `input.interact` → 触发范围交互检测

### Phase C: 资产全部接入蓝图

**目标**: 物品/技能/状态/掉落表 全部有对应的蓝图节点且可执行

1. **物品节点执行逻辑**
   - addItem / removeItem / hasItem / useItem / equipItem / unequipItem

2. **技能节点 + 注册表**
   - castSkill / skillReady / hasSkill / learnSkill / setCooldown
   - 新增节点类型到 nodeTypes.ts

3. **状态效果节点 + 组件集成**
   - applyStatus / removeStatus / hasStatus / clearAllStatus
   - Inspector 增加 StatusEffects 组件视图
   - SkillEditor 增加 statusId 引用

4. **掉落表节点 + 组件**
   - rollLoot / spawnLoot
   - 新增 Loot 组件
   - Inspector 增加 Loot 组件视图

### Phase D: 默认蓝图模板 + Inspector 完善

**目标**: 开箱即用的蓝图模板，Inspector 打通所有组件

1. **8-10 个默认蓝图模板**
   - PlayerController, SimpleNPC, EnemyPatrol, EnemyAggressive
   - Shopkeeper, TreasureChest, QuestGiver, Door, PickableItem

2. **Inspector 补全**
   - Blueprint 组件（可挂多个蓝图）
   - StatusEffects 组件（带 inline 视图）
   - Loot 组件（选择掉落表）
   - SkillBar 组件已有，确保工作正常

3. **蓝图编辑器中 preset 面板**
   - 新建蓝图时可选模板
   - 模板库浏览器

### Phase E: 打磨与集成测试

**目标**: 端到端流程可玩

1. 用默认蓝图模板搭建一个 demo 场景
2. 修复所有资产编辑器 → 蓝图 → 运行时的数据流
3. StatusEffect 的 tick/duration 在 ScriptSystem 中处理
4. 跨蓝图引用（blueprintRef 节点）工作正常

---

## 5. 文件变更清单

### 新增文件

```
packages/engine/src/
├── events/
│   ├── EventBus.ts
│   └── GameEvent.ts
├── blueprint/
│   ├── BlueprintRuntime.ts
│   ├── BlueprintContext.ts
│   └── NodeExecutors.ts
├── components/
│   ├── Blueprint.ts
│   ├── Tag.ts
│   └── Loot.ts
├── state/
│   └── GameFlags.ts
├── input/
│   └── InputActionMapper.ts
├── dialogue/
│   └── DialogueManager.ts
├── spawn/
│   └── EntitySpawner.ts
├── audio/
│   └── AudioManager.ts
└── save/
    └── SaveManager.ts

apps/editor/src/
└── blueprints/
    └── defaultTemplates.ts    (预设蓝图模板)
```

### 修改文件

```
packages/engine/src/
├── Engine.ts                  (集成 EventBus, GameFlags, 管理器, 实体生成)
├── systems/ScriptSystem.ts    (重写：事件处理 + 蓝图执行 + StatusEffects tick)
├── systems/InputSystem.ts     (保持不变，InputActionMapper 包外层)
├── systems/PlayerSystem.ts    (移除硬编码输入，保留 moveWithCollision 作为工具)
├── systems/PhysicsSystem.ts   (发射碰撞事件到 EventBus)
└── index.ts                   (导出所有新类型)

apps/editor/src/
├── canvas/PlayMode.tsx         (使用 Engine + DialogueManager UI)
├── panels/InspectorPanel.tsx   (加 Blueprint/Tag/StatusEffect/Loot 组件)
├── panels/SkillEditorPanel.tsx (add statusId to effects)
├── node-editor/nodeTypes.ts    (加资产相关节点)
└── stores/editorStore.ts       (可能的新状态)
```

---

## 6. 验收标准

1. **蓝图驱动移动** — 拖一个 PlayerController 蓝图到玩家实体，WASD 能移动，空格能跳
2. **蓝图驱动交互** — 拖一个 TreasureChest 蓝图到宝箱实体，按 E 能打开，rollLoot 执行，物品进背包
3. **蓝图驱动对话** — 拖一个 SimpleNPC 蓝图到 NPC，按 E 能显示对话
4. **资产全链路** — 在物品编辑器创建物品 → 在掉落表引用该物品 → 蓝图 rollLoot → 物品进背包
5. **状态效果全链路** — 创建状态 → 技能引用该状态 → 蓝图 castSkill → 状态施加到实体
6. **试玩模式** — PlayMode 通过 Engine 运行，共享同一套 System 代码
